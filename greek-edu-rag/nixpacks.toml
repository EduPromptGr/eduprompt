# nixpacks.toml — Railway build configuration
# Αφήνουμε το nixpacks να χειριστεί Python+pip αυτόματα.
# Μόνο ορίζουμε την εντολή εκκίνησης.

[start]
cmd = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
